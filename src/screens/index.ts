import { StyleSheet } from "react-native";

export { default as Generations } from "./Generations/Generations";

export const screenStyles = StyleSheet.create({
  container: {
    paddingTop: 16,
    paddingHorizontal: 16,
  },
});
